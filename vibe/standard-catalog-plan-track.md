# Выполнение

- [x] JPA, слой репозиториев, Kotlin JPA plugin, validate и отключение OSIV.
- [x] Сохранение авторизации, фиксации попыток кода, отзыва refresh и sync-идемпотентности.
- [x] Каталог/оборудование, ETag, архив, ревизии, аудит без владельца, административные формы.
- [x] Протокол 2 и запрет личного изменения общих UUID.
- [x] Команда check/apply; исходный UUID, атомарность, повтор и принудительный откат в тесте.
- [x] Android Room 16, сохранение ID/истории, отдельные baseline/outbox, журнал перехода.
- [x] Общие/личные метки, копии, динамическое оборудование и одноразовое APK-заполнение.
- [x] Тесты чистого восстановления, офлайна, активной тренировки и Compose accessibility.
- [x] Документация API и инструкции выпуска.
- [ ] Production backup, применение переноса и контроль реальных данных после выпуска — операция выпуска, не выполнялась в этой задаче.

## Итоговая проверка

- Backend: `./gradlew spotlessApply check bootJar` — успешно, 33 теста.
- Админка: `npm test` — 8 тестов успешно.
- Android: `./gradlew spotlessApply spotlessCheck :app:testDebugUnitTest :app:assembleDebug` — успешно, 925 тестов прошли, один существующий тест внешней копии эмулятора пропущен.
- APK: `1.3.19`, versionCode `27`. Схема Room 16 сохранена в репозитории.
- Один Android-прогон завис внутри тестового JVM; после сохранения диагностики процесс завершён, повторный полный прогон прошёл за 31 секунду без изменения набора тестов.
- `git diff --check` обоих проектов и `bash -n scripts/migrate-catalog.sh` — успешно.
